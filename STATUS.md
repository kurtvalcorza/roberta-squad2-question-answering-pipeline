# Release status

Current status: **Candidate** — E2E fine-tuning tutorial source complete, awaiting supported clean-runtime execution. The pipeline package (inference contract, digest-pinned AdversarialQA corpus, corpus exact-match/F1 with two baselines, fit check, bounded encoder fine-tuning, safetensors adapter), 36 offline unit tests plus 5 notebook-parity tests and 2 model-backed tests, one executed local CPU run of the committed notebook blob, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial `tutorials/roberta_question_answering_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). Unit tests and static conformance checks run in GitHub Actions. The tutorial remains non-release-grade until clean-runtime execution evidence from a supported hosted runtime for the exact candidate commit is recorded according to `docs/release-verification.md`.

The earlier `TASK-INFERENCE` notebook had a recorded Kaggle CPU pass (blob `5a73f16cf5cd` at `9473216`, 2026-09-14); that evidence is retained in `docs/release-verification.md` as history and does not carry over to the `E2E` notebook, which is a different blob with a different default path.

No licence or remote-code gate is open for DIMER hosting (CC-BY-4.0 weights with attribution to deepset; safetensors; `trust_remote_code=False`). The adaptation sample is fetched at run time from `adversarialqa.github.io` (AdversarialQA v1.0, CC BY-SA 3.0, one pinned 9.0 MB zip) and is not redistributed by this repository. One behaviour worth a reviewer's eye: the default corpus has no unanswerable questions, so the adapted model answers everything — the null decision the base model was trained for is traded away on that corpus, and the card, notebook and README say so.
